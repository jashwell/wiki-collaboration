<h1>My Wikis</h1>
<% @wikis.each do |wiki| %>
  <div class="media">
    <div class="media body">
      <h4 class="media-heading">
        <%= link_to wiki.title, wiki %></p>
        <%= wiki.body %>
      </h4>
    </div>
  </div>
<% end %>